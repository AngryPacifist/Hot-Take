# Database Configuration
# You need a PostgreSQL database. You can use:
# 1. Neon (cloud PostgreSQL) - https://neon.tech
# 2. Local PostgreSQL installation
# 3. Docker PostgreSQL container

DATABASE_URL=postgresql://username:password@localhost:5432/hot_take

# Session Secret (optional - has fallback)
SESSION_SECRET=your-super-secret-session-key-change-in-production

# Server Configuration (optional - defaults to 5000)
PORT=5000

# For production deployment, you might also need:
# NODE_ENV=production
